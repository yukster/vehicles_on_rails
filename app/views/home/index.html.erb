<h1>Vehicles on Rails!</h1>
<p>This is the Vehicles on Rails homepage</p>
<% if user_signed_in? %>
  <p><%= link_to 'Sign out', destroy_user_session_url, method: 'delete' %></p>
<% else %>
  <p><%= link_to 'Sign in', new_user_session_url %></p>
  <p><%= link_to 'Register', new_user_registration_url %></p>
<% end %>
